import React from "react";
import styles from "../../styles/layout/Footer.module.css";

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>
      <p>Daily Update Task Progress</p>
    </footer>
  );
};

export default Footer;
